[project]
name = 'fhdw-rag-studienberater'
version = '0.2.0'
requires-python = '>=3.11'

[tool.setuptools.packages.find]
where = ['.']
include = ['app']

[tool.pytest.ini_options]
pythonpath = ['.']

[build-system]
requires = ['setuptools>=68']
build-backend = 'setuptools.build_meta'